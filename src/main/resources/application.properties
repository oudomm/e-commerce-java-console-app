# Database Configuration
db.url=jdbc:postgresql://localhost:5432/e_commerce_db
db.username=postgres
db.password=123
db.driver=org.postgresql.Driver

# Connection Pool Settings
db.connection.timeout=10
db.socket.timeout=30
db.tcp.keepalive=true

# Application Settings
app.name=E-Commerce Console App
app.version=1.0.0